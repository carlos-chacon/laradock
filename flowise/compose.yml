services:

    flowise:
      restart: "no"
      image: flowiseai/flowise:${FLOWISE_VERSION}
      volumes:
        - flowise:/root/.flowise
      ports:
        - "${FLOWISE_HOST_PORT}:3000"
      networks:
        - frontend
        - backend
